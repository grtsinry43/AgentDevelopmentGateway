/**
 * Session UI preferences that will move into Settings Center later.
 * Keep named constants here so call sites do not hard-code product defaults.
 */

/** When true, file write/edit/delete tool blocks open with their diff visible. */
export const DEFAULT_EXPAND_FILE_TOOL_DIFF = true;
